{
  "name": "modeling-services",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:dist": "rm -rf dist",
    "build:css": "postcss ./styles/*.css --dir ./dist/styles",
    "copy:html": "cpx \"./*.html\" ./dist",
    "copy:js": "cpx \"./script/**/*.js\" ./dist/script",
    "copy:assets": "cpx \"./assets/**/*\" dist/assets",
    "minify:html": "find dist -name \"*.html\" -exec npx html-minifier-terser {} -o {} --collapse-whitespace --remove-comments --remove-redundant-attributes --remove-script-type-attributes --remove-tag-whitespace --use-short-doctype --minify-css true --minify-js true \\;",
    "minify:js": "find dist/script -name \"*.js\" -exec npx terser {} -o {} --compress --mangle \\;",
    "build": "npm run clean:dist && npm run build:css && npm run copy:html && npm run copy:js && npm run copy:assets && npm run minify:html && npm run minify:js",
    "start": "http-server ./dist",
    "deploy:s3": "aws s3 sync ./dist s3://aotamata.space --delete --acl public-read --cache-control max-age=31536000,public"
  },
  "dependencies": {
    "autoprefix": "^1.0.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.x.x",
    "autoprefixer": "^10.4.21",
    "cpx": "^1.5.0",
    "cssnano": "^7.0.7",
    "gh-pages": "^6.1.1",
    "html-minifier-terser": "^7.2.0",
    "http-server": "^14.1.1",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "4.x.x",
    "terser": "^5.43.1"
  }
}
